# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit real API keys.

# GitHub Pages base path (set to /your-repo-name/ for project pages)
# Leave blank for user/org pages (username.github.io)
VITE_BASE_PATH=/sentinel-intel/
